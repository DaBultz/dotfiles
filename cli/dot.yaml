linux:
  installs: 
    cmd: |
      sudo pacman -Sy --noconfirm base-devel ripgrep fzf zoxide lazygit eza
windows:
  installs:
    cmd: |
      scoop install ripgrep
      scoop install fzf
      scoop install zoxide
      scoop install lazygit
      scoop install eza


