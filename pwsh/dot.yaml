windows:
  # disabled due to it fails if it's already installed
  # installs: winget install Microsoft.PowerShell
  links:
    Profile.ps1:
      - ~/Documents/WindowsPowerShell/Profile.ps1
      - ~/Documents/PowerShell/Profile.ps1
